---
description: Design a feature before implementation. Assesses complexity, produces a structured plan with mermaid diagrams and a file manifest, extracts ADRs, gets your approval, then converts the plan phases into GitHub issues ready for exec-tasks.
allowed-tools: Bash, Read, Glob, Grep, Write, Edit, Skill
argument-hint: "<feature-name-or-description>"
---

Invoke the `plan-feature` skill. Pass the feature name or description as the argument. Follow the skill's workflow exactly: complexity assessment, research, plan creation, ADR extraction, user approval gate, and GitHub issue creation.
