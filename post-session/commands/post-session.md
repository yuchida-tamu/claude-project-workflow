---
description: Survey the current session for durable findings, classify each (new skill / skill update / memory / discard), and write the skill-worthy ones to disk after confirmation.
allowed-tools: Bash, Read, Glob, Grep, Write, Edit
---

Invoke the `post-session` skill to compound the lessons from this session into durable artifacts (skills, memory entries) so the next session is materially more efficient. Follow the skill's workflow exactly — including the Step 3 confirmation gate before any writes.
