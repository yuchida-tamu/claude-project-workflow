---
description: Review the GitHub project board, pick unblocked high-priority issues, spawn coding agents to execute them in parallel where safe, and open PRs that trigger @claude review.
allowed-tools: Bash, Read, Glob, Grep, Agent, Write, Edit
---

Invoke the `exec-tasks` skill to orchestrate task execution on the current project. Follow the skill's workflow exactly.
